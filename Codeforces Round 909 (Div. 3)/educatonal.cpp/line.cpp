#include <iostream>
#include <vector>
#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n ;
        vector<int>r;
        int time=0;
        for(int i=0;i<n;i++)
        {
            int a;cin>>a;
            r.push_back(a);
        }
        for(int i=0;i<r.size()-1;i++)
        {
            
        }


        
       
    }

    return 0;
}
