#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n,m;cin>>n>>m;
    vector<int>a;
    for(int i=0;i<m;i++) cin>>a[i];
    for(int i=0;i<m;i++)
    {
      int starting = a[0];
      int cnt=a[0]-1;
      if(a[i])
      
    }
    return 0;
}