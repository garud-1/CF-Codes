#include<bits/stdc++.h>
#define ll long long int
#define yes cout<<"YES"<<endl;
#define no cout<<"NO"<<endl;
#define sorta(x,n) sort(x,x+n)
#define sortd(x,n) sort(x,x+n,greater<int>())
#define arr_ip(n,x) for(int i=0;i<n;i++){ cin>>x[i]; }
#define arr_op(n,x) for(int i=0;i<n;i++){ cout<<x[i]; }
#define mod 1000000007
using namespace std;

int main()
{
    int test;
    cin>>test;
    while(test--)
    {
       
    }
    return 0;
}